# ❓ FAQ - Телеграм-бот для новостей

## ⚡ БЫСТРЫЙ СТАРТ - Импорт всех старых постов

### Команда для массового импорта

```bash
# Запусти команду
python manage.py import_forwarded_posts

# Или с автопубликацией (без модерации)
python manage.py import_forwarded_posts --auto-publish

# Или с ограничением времени (по умолчанию 5 минут)
python manage.py import_forwarded_posts --timeout 600  # 10 минут
```

### Как это работает:

1. **Запускаешь команду** - бот покажет свой username
2. **Открываешь канал** в Telegram
3. **Выбираешь нужные посты** (можно сразу несколько)
4. **Жмёшь "Переслать"** и отправляешь их боту
5. **Бот создаёт новости** автоматически с фото
6. **Повторяешь** пока не перешлешь все нужные посты
7. **Ctrl+C** для завершения

**Преимущества:**
- ✅ Работает со **старыми постами**
- ✅ Можешь **выбрать какие** посты импортировать
- ✅ Поддержка **множественных фото**
- ✅ **Мгновенное** создание новостей
- ✅ Не нужен доступ к Bot API истории

---

## Ответы на твои вопросы

### 1️⃣ Как работает бот для сбора новостей из канала?

**Бот мониторит Telegram-канал** и автоматически создаёт новости на сайте:

```bash
# Запустить бота
python manage.py run_news_bot --channel @avto_decor_news
```

**Что происходит:**
1. Ты публикуешь пост в Telegram-канале
2. Бот ловит этот пост
3. **Первая строка** поста → заголовок новости
4. **Остальной текст** → содержимое новости
5. **Первое фото** → главное изображение
6. **Остальные фото** → сохраняются в галерею статьи
7. Новость появляется на сайте!

### 2️⃣ Будут ли отображаться несколько фото?

**ДА! ✅** Теперь поддерживается **множественная загрузка фото**:

**Что сделано:**
- ✅ Создана модель `ArticleImage` для хранения нескольких фото
- ✅ Первое фото = главное (превью новости)
- ✅ Остальные фото = галерея внизу статьи
- ✅ Клик на фото → полноэкранный просмотр
- ✅ Навигация по галерее (стрелки, Escape)
- ✅ Подписи к фото (можно добавить в админке)

**Пример:**
```
Пост в Telegram:
- Текст + 5 фото

Результат на сайте:
- Заголовок из первой строки
- Главное фото (первое)
- Текст новости
- Галерея из 4 остальных фото (сетка 3 колонки)
```

### 3️⃣ Можно ли так заносить несколько фото?

**ДА! ✅** Несколько способов:

#### Способ 1: Через Telegram-канал (автоматически)

```bash
# Запусти бота
python manage.py run_news_bot --channel @avto_decor_news

# Опубликуй пост в канале с несколькими фото
# Бот автоматически:
# - Сохранит первое фото как главное
# - Остальные добавит в галерею
```

#### Способ 2: Через админку Django (вручную)

1. Открой статью в админке
2. Внизу есть секция **"Изображения статьи"**
3. Нажми **"Добавить ещё одно Изображение статьи"**
4. Загрузи фото, укажи порядок и подпись
5. Сохрани

#### Способ 3: Программно (через код)

```python
from articles.models import Article, ArticleImage

# Создай статью
article = Article.objects.create(
    title='Новая статья',
    content='Текст статьи'
)

# Добавь несколько фото
ArticleImage.objects.create(
    article=article,
    image='path/to/photo1.jpg',
    order=1,
    caption='Первое фото'
)

ArticleImage.objects.create(
    article=article,
    image='path/to/photo2.jpg',
    order=2,
    caption='Второе фото'
)
```

## 🎯 Быстрый старт

### Шаг 1: Создай канал

```
В Telegram создай канал: @avto_decor_news
```

### Шаг 2: Добавь бота в админы канала

```
1. Открой канал
2. Настройки → Администраторы → Добавить
3. Найди бота (username покажется при запуске)
4. Дай права на публикацию
```

### Шаг 3: Запусти бота

```bash
# С модерацией (черновики)
python manage.py run_news_bot --channel @avto_decor_news

# Без модерации (сразу публикуются)
python manage.py run_news_bot --channel @avto_decor_news --auto-publish
```

### Шаг 4: Публикуй посты

```
Заголовок новости

Текст новости.
Может быть многострочным.

+ прикрепи 3-5 фото
```

### Шаг 5: Проверь на сайте

```
/articles/ - список новостей
/articles/{slug}/ - статья с галереей
```

## 📷 Как работает галерея

### На странице статьи:

1. **Главное фото** вверху (большое)
2. **Текст новости**
3. **Галерея внизу** (сетка 2-3 колонки)
4. **Клик на фото** → открывается lightbox
5. **Навигация:**
   - Стрелки влево/вправо
   - Клавиши ← →
   - Escape для закрытия

### В админке:

1. **Inline-форма** с превью
2. **Порядок** сортировки
3. **Подписи** к каждому фото
4. **Drag & drop** для порядка (опционально)

## 🔥 Полезные команды

### Массовый импорт старых постов (через пересылку)

```bash
# Импорт с модерацией (черновики)
python manage.py import_forwarded_posts

# Импорт с автопубликацией
python manage.py import_forwarded_posts --auto-publish

# С увеличенным таймаутом (10 минут вместо 5)
python manage.py import_forwarded_posts --timeout 600
```

### Мониторинг новых постов (постоянная работа)

```bash
# Проверить, что бот настроен
python manage.py help run_news_bot

# Запустить бота для мониторинга канала
python manage.py run_news_bot --channel @avto_decor_news

# Запустить бота с автопубликацией
python manage.py run_news_bot --channel @avto_decor_news --auto-publish

# Запустить в фоне (Linux)
nohup python manage.py run_news_bot --channel @avto_decor_news &

# Посмотреть логи
tail -f nohup.out
```

## 🎨 Кастомизация

### Изменить количество колонок в галерее

В `templates/articles/article_detail.html`:

```html
<!-- Сейчас: 2 колонки на мобилке, 3 на десктопе -->
<div class="grid grid-cols-2 md:grid-cols-3 gap-4">

<!-- Изменить на 3 колонки везде -->
<div class="grid grid-cols-3 gap-4">
```

### Добавить подписи к фото через бот

В `articles/management/commands/run_news_bot.py`:

```python
# Найди эту часть:
ArticleImage.objects.create(
    article=article,
    order=idx
)

# Измени на:
ArticleImage.objects.create(
    article=article,
    order=idx,
    caption=f'Фото {idx + 1}'  # Автоматическая подпись
)
```

## 🛡️ Безопасность и модерация

### Режим черновиков (рекомендуется)

```bash
python manage.py run_news_bot --channel @avto_decor_news
```

- Новости сохраняются как черновики (`is_published=False`)
- Админ проверяет и публикует вручную через админку
- Защита от спама/ошибок

### Режим автопубликации

```bash
python manage.py run_news_bot --channel @avto_decor_news --auto-publish
```

- Новости публикуются сразу (`is_published=True`)
- Быстрее, но без контроля
- Используй только для проверенных каналов

## 📊 Статистика

Все новости автоматически получают:
- ✅ UUID в качестве ID
- ✅ Slug из заголовка (кириллица → транслит)
- ✅ Счётчик просмотров
- ✅ Даты создания/обновления
- ✅ Связь с галереей фото

## 🎁 Бонусы

### Что ещё умеет система:

1. **Видео** - можно добавить видео вместо фото
2. **HTML в тексте** - поддерживается форматирование
3. **Slug автоматический** - из заголовка
4. **SEO-friendly** URLs
5. **Адаптивный дизайн** - отлично на мобилках
6. **Lightbox** - красивый просмотр фото

---

**Всё готово!** 🎉

Теперь просто публикуй посты в канале, и они автоматически появятся на сайте.
