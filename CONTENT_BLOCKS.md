# Управление контент-блоками

Система управления контент-блоками позволяет редактировать текстовое содержимое страниц через админку Django без изменения кода шаблонов.

## Как использовать

### 1. В шаблонах

В шаблонах используйте template tag `get_content`:

```django
{% load content_tags %}

<h1>{% get_content 'home' 'hero_title' 'Заголовок по умолчанию' %}</h1>
<p>{% get_content 'home' 'hero_text' 'Текст по умолчанию' %}</p>
```

**Параметры:**
- `'home'` - страница (home, about, contacts, services, works, reviews)
- `'hero_title'` - ключ блока (уникальный идентификатор на странице)
- `'Заголовок по умолчанию'` - значение по умолчанию, если блок не найден

### 2. В админке

1. Зайдите в админку Django: `/admin/`
2. Найдите раздел **"Основное" → "Контент-блоки"**
3. Нажмите **"Добавить контент-блок"**

**Заполните поля:**
- **Страница**: Выберите страницу (Главная, О студии, Контакты и т.д.)
- **Ключ блока**: Уникальный ключ (например: `hero_title`, `about_text`, `detailing_subtitle`)
- **Описание**: Краткое описание для удобства (необязательно)
- **Содержимое**: Текст содержимого блока
- **Разрешить HTML**: Включите, если хотите использовать HTML-теги в содержимом

### 3. Примеры использования

#### Главная страница (home)

```django
{% load content_tags %}

<!-- Заголовок -->
<h1>{% get_content 'home' 'hero_title' 'Тюнинг-ателье Avto-Декор' %}</h1>

<!-- Подзаголовок -->
<p>{% get_content 'home' 'hero_subtitle' 'Тюнинг. Стиль. Индивидуальность.' %}</p>

<!-- Текст о компании -->
<p>{% get_content 'home' 'about_text' 'Мы предлагаем профессиональный детейлинг...' %}</p>
```

#### Страница "О студии" (about)

```django
{% load content_tags %}

<h2>{% get_content 'about' 'detailing_title' '🚗 Детейлинг в Авто-Декор' %}</h2>
<p>{% get_content 'about' 'detailing_subtitle' 'Мы предлагаем профессиональный детейлинг...' %}</p>
```

### 4. HTML в контент-блоках

Если нужно использовать HTML-теги (например, `<strong>`, `<em>`, ссылки):

1. Включите опцию **"Разрешить HTML"** при создании/редактировании блока
2. В содержимом используйте HTML:

```
Мы предлагаем <strong>профессиональный детейлинг</strong> вашего автомобиля!
```

**⚠️ Внимание:** Используйте HTML только для форматирования текста. Не используйте для вставки скриптов или небезопасного контента.

### 5. Список доступных страниц

- `home` - Главная страница
- `about` - О студии
- `contacts` - Контакты
- `services` - Услуги
- `works` - Наши работы
- `reviews` - Отзывы

### 6. Рекомендации по ключам блоков

Используйте понятные ключи, которые отражают назначение блока:

- `hero_title` - заголовок hero-секции
- `hero_subtitle` - подзаголовок hero-секции
- `about_text` - текст о компании
- `detailing_title` - заголовок про детейлинг
- `coating_title` - заголовок про покрытия
- `parts_title` - заголовок про запчасти

### 7. Поиск и фильтрация

В админке доступны:
- **Поиск** по ключу блока, описанию и содержимому
- **Фильтры** по странице, HTML-режиму, дате создания/обновления
- **Группировка** по страницам для удобства

### 8. Массовое редактирование

Можно редактировать несколько блоков одновременно:
1. Выберите нужные блоки в списке
2. Используйте действия для массовых операций

## Технические детали

### Модель ContentBlock

- `page` - страница (CharField с выбором)
- `block_key` - уникальный ключ блока
- `content` - содержимое блока (TextField)
- `is_html` - флаг разрешения HTML
- `description` - описание для админки
- `created_at` / `updated_at` - даты создания/обновления

### Template tags

- `{% get_content page block_key default %}` - получить содержимое (HTML разрешен, если включен флаг)
- `{% get_content_safe page block_key default %}` - получить содержимое с экранированием HTML

### Уникальность

Каждая комбинация `page` + `block_key` должна быть уникальной. Django автоматически проверяет это при сохранении.

## Примеры контент-блоков для главной страницы

| Страница | Ключ | Описание |
|----------|------|----------|
| home | hero_title | Заголовок hero-секции |
| home | hero_subtitle | Подзаголовок hero-секции |
| home | hero_description | Описание в hero-секции |
| home | about_subtitle | Заголовок секции "О компании" |
| home | about_description | Описание в секции "О компании" |
| home | about_text | Основной текст о компании |
| home | about_text_1 | Первый абзац о компании |
| home | about_text_2 | Второй абзац о компании |

## Примеры контент-блоков для страницы "О студии"

| Страница | Ключ | Описание |
|----------|------|----------|
| about | detailing_title | Заголовок про детейлинг |
| about | detailing_subtitle | Подзаголовок про детейлинг |
| about | coating_title | Заголовок про защитные покрытия |
| about | parts_title | Заголовок про запчасти |
| about | parts_text | Текст про запчасти |

## Миграции

После создания модели выполните:

```bash
python manage.py makemigrations
python manage.py migrate
```

## Безопасность

- HTML-контент автоматически помечается как безопасный только если включен флаг `is_html`
- Используйте `get_content_safe` для контента, который должен быть экранирован
- Не используйте контент-блоки для хранения JavaScript или небезопасного HTML
