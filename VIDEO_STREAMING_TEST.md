# 🧪 Тестирование Video Streaming

## Быстрый старт

### 1. Локальный тест (на твоём компе)

```bash
# 1. Запусти сервер Django
python manage.py runserver

# 2. В другом терминале - бот (если хочешь проверить импорт)
python manage.py run_unified_bot --channel @avto-decor --auto-publish
```

### 2. Ручное тестирование

#### Вариант A: Добавить видео через админку

1. Открой админку: http://localhost:8000/admin/
2. Создай новую статью
3. В поле `video_url` вставь `file_id` от Telegram (например: `BAACAgIAAxkBAAICZmlyJIu...`)
4. Сохрани и открой статью на сайте
5. Видео должно воспроизводиться

#### Вариант B: Импортировать из канала

1. Закинь видео в канал
2. Бот создаст статью автоматически
3. Открой статью на сайте
4. Проверь что видео играет

### 3. Тест прокси напрямую

```bash
# Получи UUID статьи из админки
# Открой в браузере:
curl -I "http://localhost:8000/stati/video/<UUID>/"

# Должен вернуть 200 OK и Content-Type: video/mp4
```

---

## Тест на сервере (production)

### 1. Деплой изменений

```bash
# SSH на сервер
ssh root@185.68.21.107

# Переходим в проект
cd ~/Avto-docer
source venv/bin/activate

# Обновляем код
git pull origin main

# Перезапускаем сервисы
sudo systemctl restart gunicorn
sudo systemctl restart telegram-bot
```

### 2. Проверка через бота

```bash
# Смотрим логи бота
sudo journalctl -u telegram-bot -f

# Закинь видео в канал через Telegram
# Должно появиться в логах:
# ✅ Видео сохранено (file_id, ~XX.XMB)
# Видео будет стримиться через прокси-сервер
```

### 3. Проверка на сайте

1. Открой https://avto-decor.com/stati/
2. Найди статью с видео
3. Кликни на неё
4. Видео должно воспроизводиться

### 4. Проверка прокси

```bash
# На сервере или с компа
curl -I "https://avto-decor.com/stati/video/<UUID>/"

# Ожидаемый результат:
# HTTP/1.1 200 OK
# Content-Type: video/mp4
# Accept-Ranges: bytes
```

---

## 🎯 Чек-лист

### ✅ Что должно работать:

- [ ] Видео < 20MB (через Bot API)
- [ ] Видео > 20MB (через прокси)
- [ ] Видео с фото (медиа-группы)
- [ ] Видео без текста (автозаголовок)
- [ ] Пересланные видео
- [ ] Несколько видео подряд
- [ ] Видео на мобильных (iOS/Android)
- [ ] Видео в разных браузерах (Chrome/Firefox/Safari)

### ❌ Что может не работать:

- [ ] Видео из приватных каналов без доступа бота
- [ ] Видео с устаревшим `file_id` (через несколько лет)
- [ ] Видео при неверном токене бота

---

## 🔍 Отладка

### Проблема: Видео не играет

**Шаг 1:** Проверь логи Gunicorn
```bash
sudo journalctl -u gunicorn -n 50 | grep video
```

**Шаг 2:** Проверь `file_id` в базе
```bash
cd ~/Avto-docer
source venv/bin/activate
python manage.py shell
```
```python
from articles.models import Article
article = Article.objects.filter(video_url__isnull=False).last()
print(f"ID: {article.id}")
print(f"file_id: {article.video_url}")
```

**Шаг 3:** Проверь доступ к Telegram API
```bash
curl "https://api.telegram.org/bot<TOKEN>/getFile?file_id=<FILE_ID>"
```

### Проблема: 404 Not Found

**Причина:** Статья не существует или не опубликована

**Решение:**
```python
# В Django shell
article.is_published = True
article.save()
```

### Проблема: 502 Bad Gateway

**Причина:** Telegram API недоступен или timeout

**Решение:**
- Проверь интернет на сервере
- Увеличь timeout в `video_proxy.py` (строка 578)

---

## 📊 Ожидаемые результаты

### Логи бота (при импорте видео)

```
📰 КАНАЛ: @avto-decor
   Пост #123
   Заголовок: "Видео от 24.01.2026 12:30:15"
   Текст: 0 символов
   Фото: 0
   Видео: Да

✅ Видео сохранено (file_id, ~15.3MB)
   Видео будет стримиться через прокси-сервер

✅ НОВОСТЬ ОПУБЛИКОВАНА: video-ot-24012026-123015
   Статус: Опубликована
   Всего обработано: 45
```

### Логи Gunicorn (при воспроизведении)

```
[INFO] Проксируем видео для статьи abc123..., file_id=BAACAgI...
[INFO] GET /stati/video/abc123.../ 200 OK
```

### Браузер (консоль разработчика)

```
Network:
  /stati/video/abc123.../  Status: 200  Type: video/mp4  Size: 15.3 MB
```

---

## 🚀 Готово к production?

### Перед деплоем проверь:

1. ✅ Все тесты пройдены локально
2. ✅ Бот сохраняет `file_id` (не пытается скачать)
3. ✅ Видео проигрывается в браузере
4. ✅ Нет ошибок в логах
5. ✅ Работает на разных размерах видео

### После деплоя проверь:

1. ✅ Gunicorn перезапущен
2. ✅ Telegram-bot перезапущен
3. ✅ Логи без ошибок
4. ✅ Видео на сайте проигрывается
5. ✅ Новые видео из канала добавляются

---

## 📞 Если что-то не работает

1. Проверь логи: `sudo journalctl -u gunicorn -f`
2. Проверь логи бота: `sudo journalctl -u telegram-bot -f`
3. Проверь токен в `.env`
4. Проверь доступ бота к каналу
5. Проверь что бот админ канала

---

**Удачи!** 🎉
