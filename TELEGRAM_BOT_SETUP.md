# Настройка Telegram бота для заявок

## Описание

Telegram бот получает заявки с контактной формы сайта и отправляет их всем подписчикам. 
Любой пользователь может подписаться на уведомления, отправив боту команду `/start`.

## Установка зависимостей

```bash
pip install -r requirements.txt
```

## Настройка

### 1. Подписка на уведомления

**Как подписаться:**

1. Запустите бота:
```bash
python manage.py run_telegram_bot
```

2. Найдите вашего бота в Telegram (по токену или username)
3. Отправьте боту команду `/start`
4. Бот автоматически добавит вас в список подписчиков

**Команды бота:**
- `/start` - Подписаться на уведомления о заявках
- `/stop` - Отписаться от уведомлений
- `/help` - Показать справку
- `/chat_id` - Показать ваш chat_id
- `/status` - Показать статус подписки

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта или установите переменные окружения:

```bash
# Токен бота (уже установлен по умолчанию)
TELEGRAM_BOT_TOKEN=8389210453:AAE0pUO2PflNa8UWqXWRN-SEnf8LvplsdrA

# Опционально: файл для хранения подписчиков (по умолчанию data/telegram_subscribers.json)
TELEGRAM_SUBSCRIBERS_FILE=/path/to/subscribers.json
```

**Примечание:** Список подписчиков автоматически сохраняется в файл `data/telegram_subscribers.json`. 
Этот файл создается автоматически при первом запуске бота.

### 3. Запуск бота

#### Локально (для тестирования)

```bash
python manage.py run_telegram_bot
```

#### На сервере (через systemd)

Создайте файл `/etc/systemd/system/avto-decor-telegram-bot.service`:

```ini
[Unit]
Description=Avto-Decor Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/Avto-docer
Environment="PATH=/root/Avto-docer/venv/bin"
Environment="DJANGO_SETTINGS_MODULE=avto_decor.settings.production"
Environment="TELEGRAM_BOT_TOKEN=8389210453:AAE0pUO2PflNa8UWqXWRN-SEnf8LvplsdrA"
Environment="TELEGRAM_CHAT_ID=your_chat_id_here"
ExecStart=/root/Avto-docer/venv/bin/python manage.py run_telegram_bot
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable avto-decor-telegram-bot

# Запустить сервис
sudo systemctl start avto-decor-telegram-bot

# Проверить статус
sudo systemctl status avto-decor-telegram-bot

# Просмотр логов
sudo journalctl -u avto-decor-telegram-bot -f
```

#### На сервере (через screen/tmux)

```bash
# Установить screen
sudo apt install screen

# Создать новую сессию
screen -S telegram-bot

# Активировать виртуальное окружение
cd /root/Avto-docer
source venv/bin/activate

# Запустить бота
python manage.py run_telegram_bot

# Отключиться от сессии: Ctrl+A, затем D
# Подключиться обратно: screen -r telegram-bot
```

## Использование

### Отправка заявок

Когда пользователь заполняет форму на странице `/kontakty`, заявка автоматически отправляется **всем подписчикам** бота.

Формат сообщения:
```
📋 Новая заявка с сайта Avto-Декор

Имя: Иван Иванов
Телефон: +7 (999) 123-45-67
Email: ivan@example.com

Сообщение:
Хочу заказать перетяжку салона...
```

### Управление подписчиками

- **Подписка:** Пользователь отправляет `/start` боту
- **Отписка:** Пользователь отправляет `/stop` боту
- **Проверка статуса:** Команда `/status` показывает, подписан ли пользователь

Список подписчиков хранится в файле `data/telegram_subscribers.json` и автоматически обновляется при подписке/отписке.

### Команды бота

- `/start` - Подписаться на уведомления о заявках
- `/stop` - Отписаться от уведомлений
- `/help` - Показать справку
- `/chat_id` - Показать ваш chat_id
- `/status` - Показать статус подписки и количество подписчиков

## Устранение неполадок

### Бот не отвечает

1. Проверьте, что бот запущен:
```bash
ps aux | grep run_telegram_bot
```

2. Проверьте логи:
```bash
# Если через systemd
sudo journalctl -u avto-decor-telegram-bot -n 50

# Если через screen
screen -r telegram-bot
```

3. Проверьте токен:
```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe
```

### Заявки не приходят

1. Проверьте, что есть подписчики (отправьте боту `/status`)
2. Проверьте, что бот запущен и работает
3. Проверьте логи Django на наличие ошибок
4. Убедитесь, что форма отправляется корректно (проверьте в браузере Network tab)
5. Проверьте файл `data/telegram_subscribers.json` - он должен содержать список подписчиков

### Ошибка "Chat not found"

Это означает, что пользователь заблокировал бота или удалил чат. Бот автоматически удаляет таких подписчиков из списка при попытке отправки сообщения.

### Нет подписчиков

Если при отправке заявки появляется предупреждение "Нет подписчиков":
1. Убедитесь, что бот запущен
2. Попросите пользователей отправить боту `/start`
3. Проверьте файл `data/telegram_subscribers.json` - он должен содержать список подписчиков

## Безопасность

⚠️ **Важно:**
- Не публикуйте токен бота в публичных репозиториях
- Используйте переменные окружения для хранения токена
- Регулярно проверяйте логи на наличие подозрительной активности
