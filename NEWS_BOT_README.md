# 📰 Telegram-бот для сбора новостей

Автоматический сбор новостей из Telegram-канала и публикация их на сайте.

## 🎯 Возможности

- **Автоматический сбор постов** из Telegram-канала
- **Поддержка множественных фото** - все фото из поста сохраняются в галерею
- **Первая строка = заголовок** - автоматическое форматирование
- **Главное фото** - первое фото становится превью новости
- **Остальные фото** - сохраняются в галерею статьи
- **Режим черновика** - новости можно сохранять как черновики для модерации
- **Автопубликация** - или публиковать сразу

## 📋 Требования

1. **Telegram-бот** (уже есть: токен в `TELEGRAM_BOT_TOKEN`)
2. **Telegram-канал** для новостей
3. **Бот должен быть администратором канала**

## 🚀 Установка

### 1. Создайте канал для новостей

```bash
# В Telegram создайте новый канал, например:
# @avto_decor_news
```

### 2. Добавьте бота в администраторы канала

1. Откройте канал
2. Настройки → Администраторы → Добавить администратора
3. Найдите вашего бота по username (смотрите в логах при запуске)
4. Дайте права на **публикацию сообщений**

### 3. Запустите бота

```bash
# Вариант 1: Указать канал через аргумент
python manage.py run_news_bot --channel @avto_decor_news

# Вариант 2: Указать ID канала
python manage.py run_news_bot --channel -1001234567890

# Вариант 3: Установить переменную окружения
export TELEGRAM_NEWS_CHANNEL=@avto_decor_news
python manage.py run_news_bot

# С автопубликацией (без модерации)
python manage.py run_news_bot --channel @avto_decor_news --auto-publish
```

### 4. Получите ID канала (если нужно)

```bash
# Способ 1: Посмотрите в логах бота после первого поста
# Способ 2: Используйте @userinfobot в Telegram
# Способ 3: Форвардните пост из канала боту @getidsbot
```

## 📝 Формат постов

### Пост с одним фото

```
Заголовок новости

Текст новости.
Может быть многострочным.

Поддерживает форматирование.
```

**Результат:**
- Заголовок: "Заголовок новости"
- Содержимое: остальной текст
- Главное фото: фото из поста

### Пост с несколькими фото

```
Новая акция на детейлинг!

С 1 февраля скидка 15% на полировку.

Успейте записаться!
```

+ 5 фото приложены

**Результат:**
- Заголовок: "Новая акция на детейлинг!"
- Содержимое: остальной текст
- Главное фото: первое фото
- Галерея: остальные 4 фото

## 🔧 Настройки

### Переменные окружения

```bash
# Токен бота (уже установлен)
TELEGRAM_BOT_TOKEN=8389210453:AAE0pUO2PflNa8UWqXWRN-SEnf8LvplsdrA

# ID или username канала
TELEGRAM_NEWS_CHANNEL=@avto_decor_news
# или
TELEGRAM_NEWS_CHANNEL=-1001234567890
```

### Аргументы команды

```bash
--channel CHANNEL       ID или username канала (обязательно)
--timeout SECONDS       Таймаут polling (по умолчанию 30)
--auto-publish          Автоматически публиковать (без модерации)
```

## 📊 Примеры использования

### 1. Запуск в режиме черновиков (с модерацией)

```bash
python manage.py run_news_bot --channel @avto_decor_news
```

Новости сохраняются как черновики. Администратор потом публикует их вручную через админку.

### 2. Запуск с автопубликацией

```bash
python manage.py run_news_bot --channel @avto_decor_news --auto-publish
```

Новости публикуются сразу, без модерации.

### 3. Запуск в фоне (Linux/Mac)

```bash
nohup python manage.py run_news_bot --channel @avto_decor_news &
```

### 4. Запуск через systemd (Linux)

Создайте файл `/etc/systemd/system/news-bot.service`:

```ini
[Unit]
Description=Telegram News Bot
After=network.target

[Service]
Type=simple
User=avto-decor
WorkingDirectory=/path/to/project
Environment="TELEGRAM_NEWS_CHANNEL=@avto_decor_news"
ExecStart=/path/to/venv/bin/python manage.py run_news_bot --auto-publish
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable news-bot
sudo systemctl start news-bot
sudo systemctl status news-bot
```

## 🎨 Отображение новостей

### На странице статьи

- **Главное фото** показывается вверху
- **Заголовок и текст** отформатированы
- **Галерея фото** внизу (если есть дополнительные фото)
- **Клик на фото** открывает полноэкранный просмотр
- **Навигация** по галерее стрелками или кнопками

### В админке

- **Inline-форма** для управления фото в галерее
- **Превью** всех изображений
- **Сортировка** по порядку
- **Подписи** к фото (можно добавить вручную)

## 🛠️ Технические детали

### Модели

**Article** (статья):
- `title` - заголовок
- `content` - содержимое
- `image` - главное изображение
- `is_published` - опубликовано?
- `views` - просмотры
- `slug` - URL (автогенерируется)

**ArticleImage** (изображение в галерее):
- `article` - связь со статьей
- `image` - изображение
- `order` - порядок отображения
- `caption` - подпись (опционально)

### API

Бот использует:
- `getUpdates` с `allowed_updates=['channel_post']`
- `getFile` для получения пути к файлу
- `https://api.telegram.org/file/bot{token}/{file_path}` для скачивания

### Обработка фото

1. **Первое фото** → `article.image` (главное)
2. **Остальные фото** → `ArticleImage` (галерея)
3. **Имена файлов**: `{slug}_0.jpg`, `{slug}_1.jpg`, ...
4. **Формат**: JPEG
5. **Хранение**: `media/articles/` и `media/articles/gallery/`

## 🐛 Устранение неполадок

### Бот не получает посты

1. **Проверьте, что бот - администратор канала**
2. **Проверьте правильность ID/username канала**
3. **Проверьте логи** - там будет информация о полученных постах

### Ошибка "Chat not found"

- Бот не добавлен в канал или не является администратором
- Неверный ID канала

### Фото не сохраняются

1. **Проверьте права на папку `media/`**
2. **Проверьте место на диске**
3. **Посмотрите логи** - там будут ошибки при скачивании

### Новости создаются дважды

- Остановите старый процесс бота
- Запустите только один экземпляр

## 📚 Дополнительно

### Как узнать ID канала?

**Способ 1**: Отправьте пост в канал и посмотрите в логах бота:

```bash
python manage.py run_news_bot --channel @your_channel
# В логах увидите: post_channel_id = -1001234567890
```

**Способ 2**: Используйте бота @userinfobot:
1. Форвардните пост из канала боту
2. Он покажет ID

**Способ 3**: Используйте API напрямую:

```bash
curl "https://api.telegram.org/bot{TOKEN}/getUpdates"
```

### Как добавить подписи к фото?

Подписи можно добавить вручную через админку:

1. Откройте статью в админке
2. В разделе "Изображения статьи" найдите нужное фото
3. Добавьте подпись в поле "Подпись"
4. Сохраните

### Миграции

Если нужно применить миграции:

```bash
python manage.py makemigrations articles
python manage.py migrate articles
```

## 🔐 Безопасность

- **Токен бота** должен храниться в переменных окружения
- **ID канала** лучше тоже хранить в переменных окружения
- **Модерация** рекомендуется - используйте режим черновиков
- **Проверка источника** - бот обрабатывает только посты из указанного канала

## 📞 Поддержка

При проблемах проверьте:
1. Логи бота (`loguru`)
2. Логи Django (`python manage.py runserver`)
3. Права на файлы и папки
4. Настройки канала и бота

---

**Готово!** 🎉

Теперь все посты из Telegram-канала будут автоматически появляться на сайте в виде новостей с фотогалереей.
