# 🔄 Логика отслеживания постов из Telegram

## Проблема

Когда бот перезапускается, он не должен создавать дубликаты новостей из уже обработанных постов.

## ✅ Решение: Модель TelegramSync

Я сделал систему отслеживания, которая **запоминает последний обработанный пост** по **3 параметрам**:

1. **`last_message_id`** - ID последнего сообщения в канале
2. **`last_post_date`** - Дата последнего обработанного поста  
3. **`last_update_id`** - ID последнего update от Telegram API

## 🎯 Как это работает

### При первом запуске:

```bash
python manage.py run_news_bot --channel @avto_decor_news
```

**Что происходит:**
1. Создаётся запись `TelegramSync` для канала `@avto_decor_news`
2. `last_message_id = None` (нет последнего поста)
3. `last_post_date = None`
4. Бот обрабатывает **ВСЕ** новые посты

### При получении поста:

```python
# Пришёл пост с message_id = 12345, дата = 21.01.2026 15:30

# 1. Проверяем, нужно ли обрабатывать:
if sync.should_process_message(message_id=12345, post_date=datetime(2026, 1, 21, 15, 30)):
    # Да, обрабатываем - это новый пост
    
    # 2. Создаём новость
    article = Article.objects.create(...)
    
    # 3. ВАЖНО: Сохраняем информацию о последнем посте
    sync.update_last_message(
        message_id=12345,
        post_date=datetime(2026, 1, 21, 15, 30),
        update_id=99999
    )
    # Теперь: last_message_id = 12345
    #         last_post_date = 21.01.2026 15:30
    #         posts_processed = 1
```

### При перезапуске:

```bash
# Перезапускаем бота
python manage.py run_news_bot --channel @avto_decor_news
```

**Что происходит:**
1. Загружается запись `TelegramSync` для канала
2. `last_message_id = 12345`
3. `last_post_date = 21.01.2026 15:30`
4. `posts_processed = 1`

```
📊 Статистика канала:
   Обработано постов: 1
   Последний пост: 21.01.2026 15:30:00
   ID последнего сообщения: 12345

♻️  Продолжаем с update_id: 99999
```

5. Когда приходит пост:
   ```python
   # message_id = 12345 (тот же пост)
   if sync.should_process_message(12345, ...):
       # False! Этот пост уже обработан
       # Пропускаем
   
   # message_id = 12346 (новый пост)
   if sync.should_process_message(12346, ...):
       # True! Это новый пост
       # Обрабатываем
   ```

## 📋 Логика проверки

### Метод `should_process_message(message_id, post_date)`

```python
def should_process_message(self, message_id: int, post_date=None) -> bool:
    # Если нет последнего ID - обрабатываем (первый раз)
    if not self.last_message_id:
        return True
    
    # Проверяем по ID сообщения (основной критерий)
    if message_id <= self.last_message_id:
        return False  # Этот пост уже был
    
    # Дополнительно проверяем по дате (двойная проверка)
    if post_date and self.last_post_date:
        if post_date <= self.last_post_date:
            return False  # Пост слишком старый
    
    return True  # Новый пост, обрабатываем
```

**Примеры:**

```python
# last_message_id = 100, last_post_date = 20.01.2026 10:00

should_process_message(99, datetime(20, 01, 09, 00))   # False (старый ID)
should_process_message(100, datetime(20, 01, 10, 00))  # False (тот же ID)
should_process_message(101, datetime(20, 01, 11, 00))  # True (новый ID и дата)
should_process_message(101, datetime(20, 01, 09, 00))  # False (новый ID, но старая дата)
```

## 🗄️ База данных

### Модель TelegramSync

```python
class TelegramSync(BaseModel):
    channel_id = CharField(unique=True)        # @avto_decor_news
    last_message_id = BigIntegerField()        # 12345
    last_post_date = DateTimeField()           # 21.01.2026 15:30
    last_update_id = BigIntegerField()         # 99999 (для offset)
    posts_processed = PositiveIntegerField()   # 47 (счётчик)
    is_active = BooleanField()                 # True/False
```

### Пример данных:

| channel_id | last_message_id | last_post_date | posts_processed | is_active |
|------------|-----------------|----------------|-----------------|-----------|
| @avto_decor_news | 12450 | 21.01.2026 15:45 | 47 | True |
| @test_channel | 5678 | 20.01.2026 12:30 | 12 | False |

## 🎛️ Управление через админку

### Просмотр статистики

```
/admin/articles/telegramsync/
```

Видно:
- Какие каналы синхронизируются
- Сколько постов обработано
- Когда был последний пост
- ID последнего сообщения
- Статус (активна/неактивна)

### Действия:

**1. Сбросить синхронизацию** (начать заново)
```
Выбрать канал → Действия → "Сбросить синхронизацию"
```
- Обнуляет `last_message_id`, `last_post_date`, `posts_processed`
- Бот начнёт обрабатывать посты заново

**2. Активировать/деактивировать**
```
Выбрать канал → Действия → "Активировать" / "Деактивировать"
```
- Деактивированные каналы бот не обрабатывает

## 🔧 Программное управление

### Получить статистику:

```python
from articles.models import TelegramSync

# Получить или создать
sync = TelegramSync.get_or_create_sync('@avto_decor_news')

# Статистика
print(f'Обработано: {sync.posts_processed}')
print(f'Последний пост: {sync.last_post_date}')
print(f'ID сообщения: {sync.last_message_id}')
```

### Проверить, нужно ли обрабатывать пост:

```python
message_id = 12346
post_date = datetime(2026, 1, 21, 16, 00)

if sync.should_process_message(message_id, post_date):
    print('Обрабатываем новый пост')
else:
    print('Пропускаем - уже обработан')
```

### Обновить информацию после обработки:

```python
# После создания новости
sync.update_last_message(
    message_id=12346,
    post_date=datetime(2026, 1, 21, 16, 00),
    update_id=100000
)
```

### Сбросить синхронизацию:

```python
sync.reset_sync()
# Теперь last_message_id = None, posts_processed = 0
```

## 🎯 Преимущества этого подхода

### 1. Нет дубликатов ✅
- Каждый пост обрабатывается **только один раз**
- Даже при перезапусках бота

### 2. Двойная проверка ✅
- По **ID сообщения** (основной критерий)
- По **дате поста** (дополнительная защита)

### 3. Персистентность ✅
- Данные хранятся в базе
- Не теряются при перезапуске
- Работают между разными запусками

### 4. Статистика ✅
- Видно сколько постов обработано
- Когда был последний пост
- История работы бота

### 5. Гибкость ✅
- Можно сбросить и начать заново
- Можно деактивировать канал
- Можно управлять через админку

## 📊 Пример работы

### Сценарий 1: Первый запуск

```bash
# 1. Запускаем бота
python manage.py run_news_bot --channel @avto_decor_news

# Вывод:
📊 Статистика канала:
   Обработано постов: 0
   Это первый запуск для этого канала

# 2. Публикуем 3 поста в канале
# Пост #100: "Новая услуга" (21.01 15:00)
# Пост #101: "Акция" (21.01 15:30)
# Пост #102: "Скидка" (21.01 16:00)

# Бот обрабатывает:
📰 НОВЫЙ ПОСТ из канала!
   Message ID: 100
   ✅ Создана новость: Новая услуга
   📊 Всего обработано: 1

📰 НОВЫЙ ПОСТ из канала!
   Message ID: 101
   ✅ Создана новость: Акция
   📊 Всего обработано: 2

📰 НОВЫЙ ПОСТ из канала!
   Message ID: 102
   ✅ Создана новость: Скидка
   📊 Всего обработано: 3

# 3. Останавливаем (Ctrl+C)
```

### Сценарий 2: Перезапуск

```bash
# 1. Перезапускаем бота
python manage.py run_news_bot --channel @avto_decor_news

# Вывод:
📊 Статистика канала:
   Обработано постов: 3
   Последний пост: 21.01.2026 16:00:00
   ID последнего сообщения: 102

♻️  Продолжаем с update_id: ...

# 2. Публикуем новый пост
# Пост #103: "Новинка" (21.01 17:00)

# Бот обрабатывает:
📰 НОВЫЙ ПОСТ из канала!
   Message ID: 103
   ✅ Создана новость: Новинка
   📊 Всего обработано: 4

# 3. Кто-то случайно пересылает старый пост #101

# Бот пропускает:
⏭️  Пост #101 уже обработан ранее, пропускаем
```

### Сценарий 3: Сброс синхронизации

```bash
# Через админку: Сбросить синхронизацию

# Перезапускаем:
python manage.py run_news_bot --channel @avto_decor_news

# Вывод:
📊 Статистика канала:
   Обработано постов: 0
   Это первый запуск для этого канала

# Бот начнёт обрабатывать ВСЕ посты заново
# (в том числе старые, если они придут через forwarding)
```

## 🐛 Устранение неполадок

### Проблема: Бот создаёт дубликаты

**Причина:** Скорее всего, несколько экземпляров бота запущено

**Решение:**
1. Убей все процессы бота
2. Запусти только один экземпляр

### Проблема: Бот пропускает новые посты

**Причина:** Синхронизация сбилась

**Решение:**
1. Открой админку: `/admin/articles/telegramsync/`
2. Найди свой канал
3. Действие → "Сбросить синхронизацию"
4. Перезапусти бота

### Проблема: Хочу импортировать старые посты

**Решение:**
```bash
# Используй команду для пересылки
python manage.py import_forwarded_posts
```

Старые посты обрабатываются через пересылку, а не через синхронизацию.

## 📝 Итог

**Теперь бот:**
- ✅ Запоминает последний обработанный пост
- ✅ Не создаёт дубликаты при перезапуске
- ✅ Проверяет по ID сообщения И дате
- ✅ Ведёт статистику обработанных постов
- ✅ Управляется через админку
- ✅ Работает надёжно и предсказуемо

**Это именно то, что ты хотел!** 🎉

Бот ловит только **новые** посты, определяя их по:
1. `message_id > last_message_id` ✅
2. `post_date > last_post_date` ✅
