# 🎬 Streaming видео из Telegram

## Описание

Вместо скачивания видео на сервер, система теперь использует **streaming proxy** — видео проксируется напрямую с серверов Telegram через наш сервер к браузеру пользователя.

## ✅ Преимущества

1. **Нет ограничений по размеру** — работает для видео любого размера
2. **Экономия места** — видео не хранится на сервере
3. **Быстрое добавление** — не нужно ждать скачивания
4. **Автоматическая работа** — бот просто сохраняет `file_id`

## 🔧 Как это работает

### 1. Бот получает видео из канала

```python
# В run_unified_bot.py и batch_import_posts.py
if video:
    file_id = video['file_id']
    article.video_url = file_id  # Сохраняем file_id вместо скачивания
    article.save()
```

### 2. Шаблон проверяет тип контента

```django
{% if article.video_url|length > 50 and not 'http' in article.video_url %}
<!-- Это file_id от Telegram - проксируем -->
<video controls>
    <source src="{% url 'articles:video_stream' article.id %}" type="video/mp4">
</video>
{% endif %}
```

### 3. View проксирует видео

```python
# articles/views/video_proxy.py
def stream_telegram_video(request, article_id):
    # 1. Получаем file_id из статьи
    # 2. Запрашиваем file_path через Bot API
    # 3. Стримим видео с серверов Telegram
    return StreamingHttpResponse(...)
```

## 📊 Что сохраняется в БД

| Поле | Значение | Пример |
|------|----------|--------|
| `video_url` | `file_id` от Telegram | `BAACAgIAAxkBAAICZmly...` |
| `video_file` | Пусто (не используется) | `None` |

## 🌐 URL структура

```
/stati/video/<uuid:article_id>/
```

Пример:
```
https://avto-decor.com/stati/video/a1b2c3d4-e5f6-7890-abcd-ef1234567890/
```

## 🎯 Поддерживаемые сценарии

### ✅ Работает:
- Видео из публичных каналов
- Видео из приватных каналов (если бот имеет доступ)
- Видео любого размера (без ограничения 20MB)
- Пересланные (forwarded) видео

### ❌ Не работает:
- Если бот не имеет доступа к каналу
- Если `file_id` устарел (через несколько лет)
- Если токен бота был изменён/отозван

## 🚀 Деплой на сервер

### 1. Обновить код

```bash
cd ~/Avto-docer
source venv/bin/activate
git pull origin main
```

### 2. Миграции (не требуются)

Изменения только в коде, БД без изменений.

### 3. Перезапустить бота

```bash
sudo systemctl restart telegram-bot
```

### 4. Перезапустить Gunicorn

```bash
sudo systemctl restart gunicorn
```

### 5. Проверить работу

```bash
# Логи бота
sudo journalctl -u telegram-bot -f

# Логи Gunicorn
sudo journalctl -u gunicorn -f
```

## 🧪 Тестирование

### 1. Локально

```bash
# Запустить сервер
python manage.py runserver

# В другом терминале - бот
python manage.py run_unified_bot --channel @your_channel --auto-publish
```

### 2. Проверить видео

1. Закинь видео в канал
2. Бот создаст статью
3. Открой статью на сайте
4. Видео должно воспроизводиться

### 3. Проверить прокси напрямую

```bash
# Получить UUID статьи из админки
# Открыть в браузере:
https://avto-decor.com/stati/video/<UUID>/
```

## 📝 Логи

### Бот сохраняет file_id:

```
✅ Видео сохранено (file_id, ~15.3MB)
   Видео будет стримиться через прокси-сервер
```

### View проксирует видео:

```
[INFO] Проксируем видео для статьи abc123..., file_id=BAACAgI...
```

### Ошибки:

```
[ERROR] Не удалось получить info для file_id=...: 400 Bad Request
[ERROR] Видео недоступно
```

## 🔐 Безопасность

1. **Проверка авторизации**: Только опубликованные статьи
2. **Кеширование**: 1 час (снижает нагрузку на Telegram API)
3. **Таймауты**: 30 секунд (защита от зависания)
4. **Валидация**: Проверка существования статьи и file_id

## 🐛 Возможные проблемы

### Видео не воспроизводится

**Причина:** Неверный `file_id` или бот не имеет доступа

**Решение:**
```bash
# Проверить логи
sudo journalctl -u gunicorn -n 50

# Проверить доступ бота к каналу
# Бот должен быть админом канала
```

### Видео долго грузится

**Причина:** Медленное соединение с Telegram API

**Решение:**
- Увеличить timeout в `video_proxy.py`
- Использовать CDN (Cloudflare)

### 400 Bad Request

**Причина:** `file_id` устарел или токен изменён

**Решение:**
- Переимпортировать видео из канала
- Проверить токен в `.env`

## 📚 Файлы изменений

1. `articles/views/video_proxy.py` — Новый view для streaming
2. `articles/urls.py` — Добавлен роут `/video/<uuid>/`
3. `articles/views/__init__.py` — Экспорт `stream_telegram_video`
4. `templates/articles/article_detail.html` — Логика определения типа видео
5. `contacts/management/commands/run_unified_bot.py` — Сохранение `file_id`
6. `articles/management/commands/batch_import_posts.py` — Сохранение `file_id`

## 🎉 Итого

- ✅ Видео любого размера работает
- ✅ Не занимает место на сервере
- ✅ Быстрое добавление новостей
- ✅ Автоматическая обработка ботом

---

**Автор:** AI Assistant  
**Дата:** 24.01.2026  
**Версия:** 1.0
