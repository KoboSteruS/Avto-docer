# 🤖 Универсальный Telegram-бот

**Один бот для всего**: заявки с сайта + новости из канала

## 🎯 Возможности

### 1. Обработка заявок с сайта
- Подписка пользователей на уведомления (`/start`)
- Отправка уведомлений о новых заявках всем подписчикам
- Управление подписками (`/stop`, `/status`, `/help`)

### 2. Сбор новостей из Telegram-канала
- Автоматический мониторинг указанного канала
- Создание новостей на сайте из постов канала
- Поддержка **медиа-групп** (несколько фото в одном посте)
- Поддержка **видео** (скачивание и сохранение)
- Главное фото + галерея дополнительных фото
- Посты без текста → автогенерация заголовка
- Отслеживание обработанных постов (нет дубликатов)

## 🚀 Быстрый старт

### Вариант 1: Только заявки (как раньше)

```bash
python manage.py run_unified_bot
```

Функции:
- ✅ Обработка команд `/start`, `/stop`, `/help`
- ✅ Уведомления о заявках
- ❌ Сбор новостей (не активен)

### Вариант 2: Заявки + Новости

```bash
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish
```

Функции:
- ✅ Обработка команд `/start`, `/stop`, `/help`
- ✅ Уведомления о заявках
- ✅ Сбор новостей из канала `@avto_decor_news`
- ✅ Автоматическая публикация новостей

## 📋 Требования

### 1. Telegram-бот
- Токен бота в переменной окружения `TELEGRAM_BOT_TOKEN`
- Бот уже настроен и работает

### 2. Для сбора новостей (опционально)
- Telegram-канал для публикаций
- Бот добавлен в администраторы канала
- Права бота: **чтение сообщений** из канала

## ⚙️ Параметры команды

| Параметр | Описание | По умолчанию | Обязательный |
|----------|----------|--------------|--------------|
| `--timeout` | Таймаут long polling (сек) | `30` | Нет |
| `--channel` | ID или username канала | Не установлен | Для новостей |
| `--auto-publish` | Автопубликация новостей | `False` (черновики) | Нет |

### Примеры:

```bash
# Заявки + новости (черновики)
python manage.py run_unified_bot --channel @avto_decor_news

# Заявки + новости (автопубликация)
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish

# Только заявки
python manage.py run_unified_bot

# С кастомным таймаутом
python manage.py run_unified_bot --timeout 60 --channel @avto_decor_news
```

## 🔧 Переменные окружения

```env
# Обязательная
TELEGRAM_BOT_TOKEN=123456:ABCdefGHI...

# Опциональная (вместо --channel)
TELEGRAM_NEWS_CHANNEL=@avto_decor_news
```

Если установлена `TELEGRAM_NEWS_CHANNEL`, можно запускать без `--channel`:

```bash
python manage.py run_unified_bot --auto-publish
```

## 📰 Формат постов для новостей

### Пост с текстом

```
Заголовок новости

Содержимое новости.
Может быть многострочным.
```

**Результат:**
- Заголовок: "Заголовок новости"
- Содержимое: остальной текст
- Первое фото → главное фото новости
- Остальные фото → галерея

### Пост только с фото (без текста)

```
[5 фото без подписи]
```

**Результат:**
- Заголовок: "Фото от 22.01.2026"
- Содержимое: "Фотография, добавленная 22.01.2026 в 15:30"
- Первое фото → главное
- Остальные 4 → галерея

### Медиа-группа (несколько фото)

Когда публикуешь пост с 5 фото:

```
Новая работа: детейлинг Porsche 911

Полировка + керамика + химчистка салона

[Фото 1] [Фото 2] [Фото 3] [Фото 4] [Фото 5]
```

**Результат:**
- 1 новость (не 5!)
- Заголовок: "Новая работа: детейлинг Porsche 911"
- Главное фото: Фото 1
- Галерея: Фото 2-5

## 🎛️ Логика работы

### 1. Получение обновлений

Бот использует **long polling**:
- Одновременно слушает **сообщения** (для заявок) и **channel_post** (для новостей)
- Таймаут по умолчанию: 30 секунд
- Нет конфликтов 409 (один процесс для всего)

### 2. Обработка сообщений от пользователей

Команды:
- `/start` → подписка на заявки
- `/stop` → отписка
- `/help` → справка
- `/chat_id` → показать chat_id
- `/status` → статус подписки

Любое другое сообщение → подтверждение получения

### 3. Обработка постов из канала

1. Проверка: пост из нужного канала?
2. Проверка: уже обработан? (по `message_id` и `last_post_date`)
3. Медиа-группа? → Собираем все фото из группы (2 сек задержка)
4. Парсинг текста:
   - Первая строка → заголовок
   - Остальное → содержимое
   - Нет текста? → Генерируем из даты
5. Создание статьи:
   - Первое фото → `article.image`
   - Остальные → `ArticleImage` (галерея)
6. Обновление синхронизации (`TelegramSync`)

### 4. Дедупликация

- **Заявки**: каждое сообщение обрабатывается один раз (по `update_id`)
- **Новости**: каждый пост обрабатывается один раз (по `message_id` + `post_date`)
- При рестарте бота: продолжает с последнего `update_id`

## 📊 Мониторинг

### Логи при запуске:

```
════════════════════════════════════════════════════════════════════════════════
🤖 УНИВЕРСАЛЬНЫЙ TELEGRAM-БОТ
════════════════════════════════════════════════════════════════════════════════
Токен: 123456:ABC...

✅ Бот подключен: @avto_decor_bot

📋 АКТИВНЫЕ ФУНКЦИИ:
   ✅ Обработка команд пользователей (/start, /help, и т.д.)
   ✅ Сбор новостей из канала

📢 Канал для новостей: @avto_decor_news
📝 Режим публикации: автоматическая

📊 Статистика канала:
   Обработано постов: 15
   Последний пост: 21.01.2026 18:30:00
   ID последнего сообщения: 12345
   ♻️  Продолжаем с update_id: 67890

👥 ПОДПИСЧИКИ НА ЗАЯВКИ:
   Файл: data/telegram_subscribers.json
   Количество: 3
   Список: ['123456789', '987654321', '555666777']

════════════════════════════════════════════════════════════════════════════════
🚀 БОТ ЗАПУЩЕН
════════════════════════════════════════════════════════════════════════════════
Ожидаю обновления...
Для остановки: Ctrl+C
```

### Логи при получении поста:

```
════════════════════════════════════════════════════════════════════════════════
📰 НОВЫЙ ПОСТ ИЗ КАНАЛА
════════════════════════════════════════════════════════════════════════════════
   Message ID: 12346
   Заголовок: Новая работа: детейлинг Porsche...
   Текст: Да
   Фото: 5 шт.

✅ Статья создана: novaya-rabota-deteiling-porsche
   📷 Главное фото сохранено
   📷 Фото 2 → галерея
   📷 Фото 3 → галерея
   📷 Фото 4 → галерея
   📷 Фото 5 → галерея
   ✅ Сохранено фото: 5/5

✅ НОВОСТЬ ОПУБЛИКОВАНА: novaya-rabota-deteiling-porsche
   Статус: Опубликована
   Всего обработано: 16
════════════════════════════════════════════════════════════════════════════════
```

### Логи при команде пользователя:

```
📨 /start от chat_id: 123456789
   ✅ Новый подписчик: 123456789. Всего: 4
```

## 🔄 Миграция со старых команд

### Было:

```bash
# Для заявок
python manage.py run_telegram_bot

# Для новостей
python manage.py run_news_bot --channel @avto_decor_news --auto-publish
```

### Стало:

```bash
# Всё в одной команде!
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish
```

### Обратная совместимость:

Старые команды **продолжают работать** и автоматически вызывают `run_unified_bot`:

```bash
# Это работает (с предупреждением)
python manage.py run_telegram_bot

# Это тоже работает (с предупреждением)
python manage.py run_news_bot --channel @avto_decor_news
```

Видишь предупреждение:
```
⚠️  ВНИМАНИЕ: Команда run_telegram_bot устарела!
⚠️  Используйте: python manage.py run_unified_bot
```

## 🛑 Остановка бота

### Локально:

```bash
Ctrl+C  # В терминале где запущен бот
```

### На сервере (systemd):

```bash
sudo systemctl stop telegram-bot
sudo systemctl restart telegram-bot
sudo systemctl status telegram-bot
```

## 📝 Production (сервер)

### 1. Создай systemd сервис

```ini
# /etc/systemd/system/telegram-bot.service
[Unit]
Description=Telegram Bot (Unified)
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/project
Environment="DJANGO_SETTINGS_MODULE=avto_decor.settings.production"
Environment="TELEGRAM_BOT_TOKEN=your_token"
Environment="TELEGRAM_NEWS_CHANNEL=@avto_decor_news"
ExecStart=/path/to/venv/bin/python manage.py run_unified_bot --auto-publish
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 2. Запусти сервис

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

### 3. Логи

```bash
# Просмотр логов
sudo journalctl -u telegram-bot -f

# Последние 100 строк
sudo journalctl -u telegram-bot -n 100
```

## ⚠️ Важные замечания

### 1. Конфликт 409

**Если получаешь ошибку 409 Conflict:**

```
Ошибка: 409 Client Error: Conflict for url: .../getUpdates
```

**Причина:** Два процесса одновременно читают обновления.

**Решение:**
```bash
# Останови все процессы Python
taskkill /F /IM python.exe  # Windows
pkill -f python             # Linux

# Запусти снова
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish
```

### 2. Медиа-группы

При публикации поста с несколькими фото:
- Telegram отправляет **несколько сообщений** с одинаковым `media_group_id`
- Бот ждёт **2 секунды** чтобы собрать все фото
- Затем объединяет их в **одну новость**

Если видишь задержку 2 сек — это нормально! Бот собирает все фото.

### 3. Посты без текста

Пост только с фото (без текста):
- ✅ **Импортируется!**
- Заголовок: "Фото от DD.MM.YYYY"
- Содержимое: автогенерация

Пост БЕЗ текста И БЕЗ фото:
- ❌ **Пропускается**

## 📚 Связанные документы

- `NEWS_BOT_README.md` — старая документация по новостям (устарела)
- `BATCH_IMPORT_GUIDE.md` — импорт старых постов пачками
- `QUICK_IMPORT.md` — быстрый импорт ~100 постов
- `MEDIA_GROUPS_INFO.md` — как работают медиа-группы
- `TELEGRAM_SYNC_LOGIC.md` — логика синхронизации

## 🎉 Итог

**Один бот** вместо двух:
- ✅ Нет конфликтов 409
- ✅ Один процесс для всего
- ✅ Один токен
- ✅ Простое управление
- ✅ Все старые команды работают (с предупреждением)

**Рекомендуемый запуск:**

```bash
# На локалке (для теста)
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish

# На сервере (через systemd)
sudo systemctl start telegram-bot
```

🚀 **Запускай и наслаждайся!**
