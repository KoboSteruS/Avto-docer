# 📦 Батч-импорт постов из Telegram

Улучшенная команда для массового импорта постов без спама и с нормальной очередью.

## 🎯 Возможности

- ✅ **Батч-обработка** — собирает все посты в очередь, потом обрабатывает
- ✅ **Без спама** — не отвечает на каждое сообщение
- ✅ **Защита от дубликатов** — не создаёт повторные новости
- ✅ **Прогресс** — показывает, сколько постов собрано/обработано
- ✅ **Умное завершение** — автоматически понимает, когда все посты получены
- ✅ **Надёжность** — обработка по частям, с паузами
- ✅ **Посты без текста** — создаёт новости даже для фото без подписи
- ✅ **Медиа-группы** — автоматически объединяет несколько фото из одного поста
- ✅ **Видео** — скачивает и сохраняет видео из постов

## 🚀 Как использовать

### Шаг 1: Останови все боты

Сначала останови `run_telegram_bot` если он запущен:

```bash
# В терминале где запущен бот нажми Ctrl+C
# Или убей процесс:
taskkill /F /IM python.exe
```

### Шаг 2: Запусти батч-импорт

```bash
# Базовый вариант (5 минут ожидания, батчи по 20 постов)
python manage.py batch_import_posts

# С автопубликацией
python manage.py batch_import_posts --auto-publish

# Увеличить время ожидания до 10 минут
python manage.py batch_import_posts --timeout 600

# Изменить размер батча (например, 10 постов за раз)
python manage.py batch_import_posts --batch-size 10

# Всё вместе
python manage.py batch_import_posts --timeout 600 --batch-size 15 --auto-publish
```

### Шаг 3: Пересылай посты

1. Открой бота в Telegram (username будет в логах)
2. Открой канал с новостями
3. Выбери посты:
   - Удерживай палец на посте
   - Выбирай другие посты (галочками)
   - Можно выбрать сразу 20-30 постов
4. Нажми «Переслать»
5. Выбери бота
6. Отправь

### Шаг 4: Жди обработки

Бот работает в два этапа:

#### Этап 1: Сбор постов (5 минут по умолчанию)

```
📥 ЭТАП 1: СБОР ПОСТОВ
   Жду 300 секунд...

   ✅ Добавлен пост #1
   ✅ Добавлено фото в группу #1
   ✅ Добавлено фото в группу #2
   ✅ Добавлено фото в группу #3
   ✅ Добавлен пост #2
   ...
   ⏱️  Собрано: 15 постов, 3 групп | Осталось: 240 сек
```

Бот подождёт, пока ты пересылаешь посты. Можно пересылать частями.

**Умное завершение:** Если прошло 30 секунд с последнего поста и уже есть посты — бот автоматически переходит к обработке.

#### Этап 2: Обработка батчами

```
✅ СБОР ЗАВЕРШЁН
📊 Постов: 15
📷 Медиа-групп: 3

🔗 Объединяю медиа-группы...
   ✅ Группа из 3 фото → 1 пост
   ✅ Группа из 2 фото → 1 пост
   ✅ Группа из 4 фото → 1 пост

📦 Всего к обработке: 18 постов

🔄 ЭТАП 2: ОБРАБОТКА ПОСТОВ
   Всего постов: 18
   Размер батча: 20

📦 Батч #1: Обработка 18 постов...

   📰 Пост #1: Приветствую, друзья!... (📝 текст, 📷 3 фото)
      📷 Главное фото сохранено
      📷 Фото 2 → галерея
      📷 Фото 3 → галерея
      ✅ Сохранено фото: 3/3
      ✅ Создана: privetstvuyu-druzya
   📷 Пост #2: Фото от 15.01.2026... (только фото: 2 шт.)
      📷 Главное фото сохранено
      📷 Фото 2 → галерея
      ✅ Сохранено фото: 2/2
      ✅ Создана: foto-ot-15012026
   📰 Пост #3: Доброго дня, друзья!... (📝 текст, 📷 4 фото)
      📷 Главное фото сохранено
      📷 Фото 2 → галерея
      📷 Фото 3 → галерея
      📷 Фото 4 → галерея
      ✅ Сохранено фото: 4/4
      ✅ Создана: dobrogo-dnya-druzya
   ⏭️  Пост #4: "Тестовая новость" уже существует
   ...

   Батч #1 завершён
   Создано: 18 | Пропущено: 2 | Ошибок: 0
```

## 📊 Параметры

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `--timeout` | 300 (5 мин) | Сколько ждать посты |
| `--batch-size` | 20 | Сколько постов обрабатывать за раз |
| `--auto-publish` | False | Публиковать сразу или как черновики |

## 💡 Рекомендации

### Для 100 постов:

```bash
# Вариант 1: Несколько заходов по 20-30 постов
python manage.py batch_import_posts --timeout 180 --batch-size 20 --auto-publish

# Пересылаешь 20-30 постов
# Ждёшь обработки
# Запускаешь снова
# Пересылаешь следующие 20-30 постов
```

### Для быстрого импорта:

```bash
# Короткое ожидание, большие батчи
python manage.py batch_import_posts --timeout 120 --batch-size 30 --auto-publish

# Пересылай быстро все посты сразу
```

### Для надёжности:

```bash
# Длинное ожидание, маленькие батчи
python manage.py batch_import_posts --timeout 600 --batch-size 10 --auto-publish

# Пересылай спокойно, по частям
```

## 🆚 Сравнение с другими командами

| Команда | Плюсы | Минусы |
|---------|-------|--------|
| `batch_import_posts` | ✅ Батчи<br>✅ Без спама<br>✅ Умная очередь<br>✅ Для массового импорта | ⚠️ Нужно пересылать вручную |
| `import_forwarded_posts` | ✅ Простая | ❌ Спамит ответами<br>❌ Нет батчей<br>❌ Медленная |
| `run_news_bot` | ✅ Автоматическая<br>✅ Для новых постов | ❌ Только новые посты<br>❌ Не для истории |

## 🐛 Проблемы и решения

### Проблема: Бот спамит "Вы подписаны на уведомления"

**Причина:** Запущен `run_telegram_bot` одновременно с импортом

**Решение:**
```bash
# Останови run_telegram_bot
# Ctrl+C в его терминале

# Или убей все процессы Python:
taskkill /F /IM python.exe

# Запусти только batch_import_posts
python manage.py batch_import_posts --auto-publish
```

### Проблема: Посты не импортируются

**Причина:** Отправляешь обычные сообщения, а не пересланные

**Решение:** 
- Обязательно используй кнопку «Переслать» (Forward) из канала
- Не копируй текст — именно пересылай

### Проблема: "Нет текста, пропускаем"

**Причина:** Старая версия команды не обрабатывала фото без текста

**Решение:**
- Обновлена команда — теперь создаёт новости для фото без подписи
- Заголовок генерируется автоматически: "Фото от 21.01.2026"
- Посты без текста И без фото — пропускаются (пустые посты)

### Проблема: Дубликаты

**Причина:** Уже импортированные посты

**Решение:** 
- Бот автоматически пропускает дубликаты по заголовку
- Увидишь: `⏭️ Пост #3: "..." уже существует`

### Проблема: Фото не загружаются

**Причина:** Сетевые проблемы или большие файлы

**Решение:**
- Подожди, бот повторит попытку
- Проверь интернет
- Уменьши размер батча: `--batch-size 10`

## 📝 Примеры использования

### Пример 1: Импорт 20 постов

```bash
# Запусти
python manage.py batch_import_posts --timeout 180 --batch-size 20 --auto-publish

# Увидишь username бота в логах
# Открой бота в Telegram
# Выбери 20 постов из канала
# Переслать → выбери бота → Отправь

# Жди:
# ✅ Добавлен пост #1
# ✅ Добавлен пост #2
# ...
# ✅ Добавлен пост #20

# Через 30 сек после последнего:
# ✅ СБОР ЗАВЕРШЁН: 20 постов
# 🔄 ЭТАП 2: ОБРАБОТКА ПОСТОВ
# ...
# ✨ ИМПОРТ ЗАВЕРШЁН
# ✅ Создано: 18
# ⏭️ Пропущено: 2 (дубликаты)
```

### Пример 2: Импорт 100 постов по частям

```bash
# Заход 1: 30 постов
python manage.py batch_import_posts --timeout 120 --batch-size 30 --auto-publish
# Пересылаешь 30 постов
# Ждёшь "ИМПОРТ ЗАВЕРШЁН"

# Заход 2: ещё 30 постов
python manage.py batch_import_posts --timeout 120 --batch-size 30 --auto-publish
# Пересылаешь следующие 30 постов

# Заход 3: ещё 30 постов
python manage.py batch_import_posts --timeout 120 --batch-size 30 --auto-publish
# Пересылаешь следующие 30 постов

# Заход 4: последние 10 постов
python manage.py batch_import_posts --timeout 120 --batch-size 10 --auto-publish
# Пересылаешь последние 10 постов

# Итого: 100 постов импортировано
```

### Пример 3: Быстрый импорт всех постов сразу

```bash
# Запусти с большим таймаутом
python manage.py batch_import_posts --timeout 600 --batch-size 25 --auto-publish

# Открой канал
# Выбери ВСЕ 100 постов (прокручивай и выбирай)
# Переслать → боту

# Бот соберёт все 100 постов
# Затем обработает батчами по 25
```

## ✅ Чеклист для импорта

- [ ] Остановлен `run_telegram_bot`
- [ ] Убиты все лишние процессы Python
- [ ] Запущен `batch_import_posts` с нужными параметрами
- [ ] Узнал username бота из логов
- [ ] Открыл бота в Telegram
- [ ] Открыл канал с новостями
- [ ] Выбрал посты (удерживай палец → галочки)
- [ ] Нажал «Переслать»
- [ ] Выбрал бота
- [ ] Отправил
- [ ] Жду сбор постов
- [ ] Жду обработку батчами
- [ ] Проверил результат в админке `/admin/articles/article/`

## 🎉 Итог

Теперь у тебя есть:
- ✅ Нормальная команда без спама
- ✅ Батч-обработка с очередью
- ✅ Можно импортировать 20-30 постов комфортно
- ✅ Для 100 постов — делай 3-4 захода
- ✅ Защита от дубликатов
- ✅ Показывает прогресс

**Рекомендую:** Для твоих ~100 постов делай по 25-30 постов за раз, 4 захода.
