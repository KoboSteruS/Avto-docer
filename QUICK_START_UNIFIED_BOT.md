# ⚡ Быстрый старт: Универсальный бот

## 🎯 Одна команда для всего

```bash
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish
```

Это заменяет:
- ❌ `run_telegram_bot` (заявки)
- ❌ `run_news_bot` (новости)

Теперь:
- ✅ `run_unified_bot` (всё сразу!)

## 📋 Три режима работы

### 1. Только заявки

```bash
python manage.py run_unified_bot
```

### 2. Заявки + Новости (черновики)

```bash
python manage.py run_unified_bot --channel @avto_decor_news
```

### 3. Заявки + Новости (автопубликация)

```bash
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish
```

## 🚀 Для сервера

### Останови старый бот:

```bash
# Останови все Python процессы
sudo pkill -f python

# Или останови systemd сервис
sudo systemctl stop telegram-bot
```

### Запусти новый:

```bash
python manage.py run_unified_bot --channel @avto_decor_news --auto-publish
```

## ✅ Что получаешь

- ✅ Нет конфликтов 409
- ✅ Заявки работают
- ✅ Новости из канала работают
- ✅ Поддержка медиа-групп (несколько фото)
- ✅ Один процесс вместо двух
- ✅ Все старые команды работают (с предупреждением)

## 📖 Подробная документация

См. `UNIFIED_BOT_README.md`

## 🎉 Готово!

Запускай и всё работает! 🚀
