# 🚀 Быстрый старт Telegram бота

## ⚠️ ВАЖНО: Бот должен работать постоянно!

Бот должен быть запущен **все время**, чтобы отслеживать команды `/start` и сохранять подписчиков.

## Шаги для запуска:

### 1. Запустите бота

```bash
python manage.py run_telegram_bot
```

**НЕ ЗАКРЫВАЙТЕ** этот терминал! Бот должен работать постоянно.

### 2. Найдите бота в Telegram

- Найдите вашего бота по username или токену
- Или используйте ссылку: `https://t.me/ваш_бот_username`

### 3. Отправьте команду `/start`

В Telegram отправьте боту:
```
/start
```

Бот ответит:
```
✅ Вы успешно подписаны на уведомления о заявках!

Теперь все заявки с сайта Avto-Декор будут приходить в этот чат.

Ваш chat_id: 123456789
Всего подписчиков: 1
```

### 4. Проверьте подписчиков

В **другом терминале** (не закрывая бота) выполните:

```bash
python manage.py check_subscribers
```

Должно показать:
```
Всего подписчиков: 1

Список подписчиков:
  1. 123456789
```

### 5. Протестируйте отправку заявки

1. Откройте сайт: `http://127.0.0.1:8000/kontakty`
2. Заполните форму заявки
3. Отправьте форму
4. Проверьте Telegram - должно прийти уведомление!

## 🔧 Если бот не видит подписчиков:

### Проверка 1: Бот запущен?

```bash
ps aux | grep run_telegram_bot
```

Если процесса нет - запустите бота!

### Проверка 2: Файл создан?

```bash
ls -la data/telegram_subscribers.json
cat data/telegram_subscribers.json
```

Если файла нет - отправьте боту `/start` еще раз.

### Проверка 3: Подписчики в файле?

```bash
python manage.py check_subscribers
```

## 📱 Для нескольких пользователей:

Каждый пользователь должен:
1. Найти бота в Telegram
2. Отправить `/start`
3. Бот автоматически добавит его в список

Все подписчики будут получать уведомления о заявках!

## 🖥️ Запуск на сервере (постоянно):

### Вариант 1: Systemd (рекомендуется)

См. инструкцию в `TELEGRAM_BOT_SETUP.md`

### Вариант 2: Screen

```bash
# Создайте новую сессию
screen -S telegram-bot

# Запустите бота
cd /root/Avto-docer
source venv/bin/activate
python manage.py run_telegram_bot

# Отключитесь: Ctrl+A, затем D
# Подключитесь обратно: screen -r telegram-bot
```

### Вариант 3: Nohup

```bash
nohup python manage.py run_telegram_bot > bot.log 2>&1 &
```

## ⚡ Команды бота:

- `/start` - Подписаться на уведомления
- `/stop` - Отписаться
- `/status` - Проверить статус
- `/help` - Справка
- `/chat_id` - Показать ваш chat_id

## 🐛 Отладка:

Если что-то не работает:

1. Проверьте, что бот запущен
2. Проверьте логи бота
3. Проверьте подписчиков: `python manage.py check_subscribers`
4. Отправьте боту `/start` заново
5. Проверьте файл `data/telegram_subscribers.json`

## 📝 Логи:

Бот выводит подробные логи:
- Когда получает команды
- Когда добавляет подписчиков
- Когда отправляет сообщения
- Ошибки (если есть)

Следите за логами, чтобы видеть, что происходит!
